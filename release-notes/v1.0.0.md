# Tinify for Mac 1.0.0

A native workspace for compressing, resizing, cropping, and converting images locally on Apple silicon Macs, with transparent output reporting.

## Highlights

- Compress, resize, crop, and convert from one focused Mac workspace.
- Process image content locally and keep the queue as the clear source of truth for progress, cancellation, retry, and results.
- See honest outcomes: compression never writes a larger duplicate, while requested transformations report any size increase plainly.

## Improvements

- Save reusable presets and apply the same bounded settings from the app, Finder, or watched-folder automations.
- Review durable Activity and local Impact totals for completed, validated outputs.
- Link an optional Tinify account for entitled preset sync, automations, device management, and privacy-bounded Impact sync.

## Privacy and security

- Image and file content stays on the Mac during processing.
- Sensitive account and bookmark material is protected through macOS Keychain and sandboxed file access.
- Exported support diagnostics exclude images, filenames, paths, bookmarks, tokens, account identifiers, and raw logs.
- The app and installer are signed with Developer ID, notarized by Apple, and updated through a cryptographically signed Sparkle feed.

## Compatibility

- Requires macOS 14 or later on an Apple silicon Mac.
- The Finder action can be enabled in macOS extension settings after installation.
- Local manual processing does not require a Tinify account; synchronized and paid capabilities require a linked, entitled account.

## Known issues

- The public Intel build is withheld until separate physical Intel and filesystem validation is complete.
- VoiceOver and full keyboard-navigation coverage for some Optimize controls is still being improved.
- macOS may request file or folder access when a new location is selected for the first time.
